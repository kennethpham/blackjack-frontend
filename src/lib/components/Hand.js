/**
 *
 * @typedef { import('$lib/backend/models').UserData } UserData
 * @typedef { import('$lib/components/Card').Card } Card
 *
 * @typedef {Object} Hand
 * @property {UserData} user
 * @property {Array.<Card>} cards
 */

module.exports = {};
